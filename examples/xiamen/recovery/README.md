# Xiamen character recovery archive

These files are retained for traceability and are excluded from production counts.

- `legacy-watercolor-v1/`: rejected after user review; realistic, elongated watercolor-fashion style did not match the shared city-character system.
- `style-repair-v2/`: intermediate targeted repair; closer to the shared system but still too elongated.
- `accepted-v3-working/`: chroma-key source and versioned working outputs used to build the accepted canonical `../xiamen_character_*.png` files.

Production candidate: `../xiamen_character_transparent.png`.
